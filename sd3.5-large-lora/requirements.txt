# --prefer-binary
spaces
gradio[oauth]
#--pre
#--extra-index-url https://download.pytorch.org/whl/cu129
numpy
#opencv-python
# Pin the whole torch stack together. torchvision/torchaudio must come from the
# same CUDA build as torch: an unpinned torchaudio resolves to a cu13 wheel whose
# _torchaudio.so links libcudart.so.13, which is absent next to a cu12 torch.
# transformers imports torchaudio eagerly from loss/loss_rnnt.py whenever the
# package is importable, so the broken .so surfaces as an ImportError inside
# `from diffusers import StableDiffusion3Pipeline` and the Space never boots.
# torchaudio is unused here (text-to-image only) — left out entirely.
torch==2.8.0
torchvision==0.23.0

tokenizers
safetensors
hf-transfer
huggingface_hub>=0.20.0
sentencepiece
paramiko
google-cloud-storage

git+https://github.com/asomoza/image_gen_aux.git
git+https://github.com/ford442/cyper.git

peft

git+https://github.com/huggingface/accelerate.git
transformers
git+https://github.com/huggingface/diffusers.git

#bitsandbytes
#git+https://github.com/huggingface/optimum.git
#git+https://github.com/facebookresearch/xformers.git

kernels